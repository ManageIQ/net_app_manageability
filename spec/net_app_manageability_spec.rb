require 'spec_helper'

describe NetAppManageability do
  it 'has a version number' do
    expect(NetAppManageability::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
